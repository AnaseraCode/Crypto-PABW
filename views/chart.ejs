<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Coin Chart: <%= symbol %></title>
  <style>
    body {
      margin: 0;
      padding: 0;
      background-color: #121212; /* Atur warna latar belakang menjadi #121212 */
    }
    h1 {
      font-family: 'Poppins', sans-serif; /* Gunakan font Poppins */
      font-weight: bold; /* Atur tebal font menjadi bold */
      color: white; /* Atur warna teks judul menjadi putih */
      text-align: center; /* Posisikan judul di tengah halaman */
    }
    .tradingview-widget-container {
      width: 100%; /* Atur lebar kontainer chart menjadi 100% */
    }
  </style>
</head>
<body>
  <h1>Coin Chart: <%= symbol %></h1> <!-- Judul halaman dengan simbol koin yang ditampilkan -->
  <!-- TradingView Widget BEGIN -->
  <div class="tradingview-widget-container">
    <div class="tradingview-widget-container__widget" style="height: calc(100vh - 72px);"></div>
    <!-- Kontainer untuk chart, tingginya diatur agar mengambil tinggi viewport dikurangi 72px (tinggi judul) -->
    <div class="tradingview-widget-copyright">
      <a href="https://www.tradingview.com/" rel="noopener nofollow" target="_blank">
        <span class="blue-text"></span>
      </a>
    </div>
  </div>
  <script type="text/javascript">
    var symbol = "<%= symbol %>"; // Mendapatkan simbol koin dari server
    var script = document.createElement("script"); // Membuat elemen script
    script.type = "text/javascript"; // Atur tipe script
    script.src = "https://s3.tradingview.com/external-embedding/embed-widget-advanced-chart.js"; // Sumber script chart
    script.async = true; // Atur agar script dijalankan secara asynchronous
    script.innerHTML = JSON.stringify({
      "autosize": true, // Atur agar ukuran chart menyesuaikan otomatis
      "symbol": symbol, // Atur simbol koin yang akan ditampilkan di chart
      "interval": "D", // Interval waktu (H = Hourly, D = Daily, W = Weekly, M = Monthly)
      "timezone": "Etc/UTC", // Zona waktu chart
      "theme": "dark", // Tema chart (dark/light)
      "style": "1", // Gaya chart
      "locale": "en", // Bahasa chart
      "allow_symbol_change": true, // Izinkan perubahan simbol koin
      "details": true, // Tampilkan detail chart
      "calendar": false, // Sembunyikan kalender
      "support_host": "https://www.tradingview.com" // Host dukungan TradingView
    });
    document.querySelector('.tradingview-widget-container__widget').appendChild(script); // Menambahkan script chart ke dalam kontainer
  </script>
  <!-- TradingView Widget END -->
</body>
</html>